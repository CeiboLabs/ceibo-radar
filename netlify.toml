[build]
  command = "npm run build"
  publish = ".next"

[build.environment]
  # Skip Playwright Chromium download on Netlify — scraper runs locally only
  PLAYWRIGHT_SKIP_BROWSER_DOWNLOAD = "1"
  NODE_VERSION = "20"

[[plugins]]
  package = "@netlify/plugin-nextjs"
